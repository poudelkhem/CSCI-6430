Assuming you have spark installed on your machine, simply run the Jupyter notebook cells to see the model in action. 

The in progress models will break right now, but they will be fixed with the next version.